import type { Snapshot } from './presets';

export const presetDefaults: Record<string, (Snapshot | null)[]> = {
  // To populate: open the app, set up your 1/2/3 slots, then use Options → Developer → "Copy Defaults"
  // and paste the exported object body here.
};
